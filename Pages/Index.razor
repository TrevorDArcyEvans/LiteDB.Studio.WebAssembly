@page "/"

@using BlazorMonaco

<style>
  .monaco-editor-container 
  { 
    min-height: 300px;
    border-width: 1px;
    border-color: black;
    border-style: solid;
  }
</style>

<PageTitle>LiteDB Studio (WebAssembly version)</PageTitle>

<ContextMenu Id="collectionsMenu">
  <Item OnClick="@(async () => await OnAll())" Enabled="_enableCollMenu">All</Item>
  <Item OnClick="@(async () => await OnCount())" Enabled="_enableCollMenu">Count</Item>
  <Item OnClick="@(async () => await OnExplainPlan())" Enabled="_enableCollMenu">Explain plan</Item>
  <Seperator/>
  <Item OnClick="@(async () => await OnIndexes())" Enabled="_enableCollMenu">Indexes</Item>
  <Item OnClick="@(async () => await OnExport())" Enabled="_enableCollMenu">Export</Item>
  <Item OnClick="@(async () => await OnAnalyse())" Enabled="_enableCollMenu">Analyse</Item>
  <Item OnClick="@(async () => await OnRename())" Enabled="_enableCollMenu">Rename</Item>
  <Item OnClick="@(async () => await OnDrop())" Enabled="_enableCollMenu">Drop</Item>
</ContextMenu>

<MudPaper Elevation="25">
  <MudToolBar>
    <MudButton Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.OpenInBrowser" OnClick="@(async () => await OnOpen())">Open...</MudButton>
    <MudButton Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.Refresh" OnClick="OnRefresh">Refresh</MudButton>
    <MudButton Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.DirectionsRun" OnClick="@OnRun">Run</MudButton>
    <MudButton Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.Start" OnClick="OnBegin">Begin</MudButton>
    <MudButton Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.SaveAlt" OnClick="OnCommit">Commit</MudButton>
    <MudButton Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.Undo" OnClick="OnRollback">Rollback</MudButton>
    <MudButton Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.SafetyCheck" OnClick="OnCheckpoint">Checkpoint</MudButton>
  </MudToolBar>
</MudPaper>

<MudGrid>
  <MudItem xs="2">
    <MudTreeView T="string" Items="_collections" SelectedValueChanged="OnCollectionSelected">
      <MudTreeViewItem T="string" Text="@_fileName">
        @foreach (var coll in _collections)
        {
          <ContextMenuTrigger MenuId="collectionsMenu">
            <MudTreeViewItem T="string" Text="@coll" Value="@coll"/>
          </ContextMenuTrigger>
        }
      </MudTreeViewItem>
    </MudTreeView>
  </MudItem>

  <MudItem xs="10">
    <MudDynamicTabs
      Elevation="4"
      Rounded="true"
      ApplyEffectsToContainer="true"
      PanelClass="px-4 py-6"
      AddTab="AddTabCallback"
      CloseTab="@(panel => CloseTabCallback(panel))"
      AddIconToolTip="Click here to add a new tab"
      CloseIconToolTip="Close this tab. All data will be lost"
      @bind-ActivePanelIndex="_activeTabIndex">
      @foreach (var item in _tabs)
      {
        <MudTabPanel Text="@item.Name" Tag="@item.Id">
          <ChildContent>
            <MudStack>
              <MonacoEditor
                @ref="@item.Query"
                Id="@item.Id.ToString()"
                ConstructionOptions="SQL_EditorOptions"/>
              <MudTabs Elevation="2" Rounded="true">
                <MudTabPanel Text="Grid">
                  <MudTable Items="@item.Results" Breakpoint="Breakpoint.Sm">
                    <HeaderContent>
                      @{
                        var cols = item.Results.FirstOrDefault()?.Keys ?? Enumerable.Empty<string>();
                        foreach (var col in cols)
                        {
                          <MudTh>@col</MudTh>
                        }
                      }
                    </HeaderContent>
                    <RowTemplate>
                      @{
                        var cols = item.Results.FirstOrDefault()?.Keys ?? Enumerable.Empty<string>();
                        foreach (var col in cols)
                        {
                          <MudTd DataLabel="@col">@context[col].ToString()</MudTd>
                        }
                      }
                    </RowTemplate>
                  </MudTable>
                </MudTabPanel>
                <MudTabPanel Text="Text">
                  <MudTextField @bind-Value="@item.ResultsJson" Lines="20"/>
                </MudTabPanel>
                <MudTabPanel Text="Parameters">
                  <MudTextField @bind-Value="@item.Parameters" Lines="10"/>
                </MudTabPanel>
              </MudTabs>
            </MudStack>
          </ChildContent>
        </MudTabPanel>
      }
    </MudDynamicTabs>
  </MudItem>
</MudGrid>
